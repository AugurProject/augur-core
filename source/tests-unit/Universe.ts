import { expect } from "chai";

describe("Universe", () => {
    it("#getTypeName()", async () => {
        // TODO: figure out how we want to do unit testing
        expect(true).to.equal(true);
    });
});
