dist: trusty
sudo: required
language: javascript
services:
  - docker
notifications:
  email: false
before_install:
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  - sudo apt-get update
  - sudo apt-get -y -o Dpkg::Options::="--force-confnew" install docker-ce
  - sudo sudo curl -L https://github.com/docker/compose/releases/download/1.17.0/docker-compose-`uname -s`-`uname -m` -o /usr/local/bin/docker-compose
  - sudo chmod +x /usr/local/bin/docker-compose
script: docker pull $IMAGE_BUILD;
        docker tag $IMAGE_BUILD $IMAGE_LATEST;
        if [[ "$TESTS" == "integration:geth" ]]; then
        npm run docker:run:test:integration:geth;
        elif [[ "$TESTS" == "integration:parity" ]]; then
        npm run docker:run:test:integration:parity;
        elif [[ "$TESTS" == "security:mythril" ]]; then
        npm run docker:run:test:security:mythril;
        cat source/contracts/test-results.log;
        elif [[ "$TESTS" == "security:maian" ]]; then
        npm run docker:run:test:security:maian;
        elif [[ "$TESTS" == "security:smt" ]]; then
        npm run docker:run:test:security:smt;
        else
          npm run docker:run:test:unit -- $TESTS;
        fi
jobs:
  include:
    - stage: build
      script: docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD";
              docker pull $IMAGE_LATEST;
              npm run docker:build;
              docker tag $IMAGE_LATEST $IMAGE_BUILD;
              docker push $IMAGE_BUILD
      env:
        - DOCKER_USERNAME=augurintegration
        - secure: "Pyod58zXv4R7HjAmlEOhc2HzjWTDVK2olgvZ2EZEyzNKuhbTPeL/yb+bCFkrmx2BZIEXTqFNpBQtCnjopT4f/mXm9VlAoIcLQ78FSVOEHmtg5ndyMqoTsVxeqcTOMd8QRXAz8VanKZ/+suf2+rvI5C9REU5VVPyxGBnfolDHzGQ="
    - stage: deploy
      script: skip
      env:
        - DOCKER_USERNAME=augurintegration
        - secure: "Pyod58zXv4R7HjAmlEOhc2HzjWTDVK2olgvZ2EZEyzNKuhbTPeL/yb+bCFkrmx2BZIEXTqFNpBQtCnjopT4f/mXm9VlAoIcLQ78FSVOEHmtg5ndyMqoTsVxeqcTOMd8QRXAz8VanKZ/+suf2+rvI5C9REU5VVPyxGBnfolDHzGQ="
        - TESTS=
      deploy: &script
        provider: script
        skip_cleanup: true
        script: docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD";
                docker pull $IMAGE_BUILD;
                docker tag $IMAGE_BUILD $IMAGE_LATEST;
                docker push $IMAGE_LATEST
      if: type = push
stages:
  - build
  - test
  - name: deploy
    if: branch = master or tag =~ ^v\d+\.\d+\.\d+(-\d+)?$
git:
  depth: 5
branches:
  only:
    - master
    - /^v\d+\.\d+\.\d+(-\d+)?$/
env:
  global:
    - IMAGE_BUILD=augurproject/augur-core:build.${TRAVIS_BUILD_NUMBER}
    - IMAGE_LATEST=augurproject/augur-core:latest
  matrix:
    - TESTS="tests/test*.py tests/libraries tests/trading"
    - TESTS="tests/reporting"
    - TESTS="tests/fuzzy"
    - TESTS="tests/unit"
    - TESTS="integration:geth"
    - TESTS="integration:parity"
    - TESTS="security:mythril"
    - TESTS="security:maian"
    - TESTS="security:smt"
