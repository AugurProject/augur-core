sudo: true
language: python
notifications:
  email: false
python:
  - "2.7"
install:
  - sudo apt-get install build-essential libpython-dev libblas-dev liblapack-dev libgmp-dev libmpfr-dev libmpc-dev gfortran python-numpy python-matplotlib python-scipy -y -qq
  - pip install --upgrade pip
  - pip install -r requirements.txt
  - pip install -r test_requirements.txt
script:
  - ./runtests.sh
