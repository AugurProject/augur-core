macro Controller: 0xC001D00D
data foo

def ok(x):
    y = array(3)
    blah()
    n = create('functions/shareTokens.se')
    return(y[2])
    
macro blah():
    $x = array(5)
    $i = 0
    $x[$i] = 3

macro safeToSubtract($a, $b):
    ($b <= $a && (($a - $b) <= $a))


macro safeToSubtract($a, $b):
    ($b <= $a && (($a - $b) <= $a))
