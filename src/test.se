Controller = 0xC001D00D

event logit(returnValue)

def init():
    self
    log(type = logit, Controller)
    
def ok(y, b):
    size = ~calldatasize()
    # a = alloc(size + 28)
    a = array((size + 28)/32)
    ~calldatacopy(a + 28, 0, size)
    return(a, items=(size + 28)/32)
