macro Controller: 0xC001D00D
data foo

def ok(x):
    send(0, 5)
    if(block.timestamp < 5055):
        return(2)
    self.hmm()
    return(3)

def hmm():
    return(5)

macro safeToSubtract($a, $b):
    ($b <= $a && (($a - $b) <= $a))


macro safeToSubtract($a, $b):
    ($b <= $a && (($a - $b) <= $a))
