macro safeToSubtract($a, $b):
    ($b >= $a)

macro safeToSubtract($a, $b):
    ($b <= $a)

def ok():
    if(safeToSubtract(10, 5)):
        return(1)
    else:
        return(0)

def heh():
    x
    x += 2
    return(5)