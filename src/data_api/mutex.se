data mutex

data controller

def setMutex():
    self.controller.checkWhitelist(msg.sender)
    self.mutex = 1
    return(1)

def unsetMutex():
    self.controller.checkWhitelist(msg.sender)
    self.mutex = 0
    return(1)

def getMutex():
    return(self.mutex)
