Adding contract to Geth chain guide:

var evm = '0x...';
var coinbase = eth.coinbase
var gas_amount = a big number
var address = eth.sendTransaction({from: coinbase, gas: gas_amount, code: evm});
var desc = serpent mk_full_signature file.se;
var Contract = web3.eth.contract(desc);
contract = new Contract(address);
contract.call({"from": coinbase}).faucet().toNumber();
eth.getCode(address);

