# Arithmetic mean of an array
macro mean($a:$asz):
    with total = 0:
        with i = 0:
            while i < $asz:
                total += $a[i]
                i += 1
        total * 0x10000000000000000 / i

def test_mean(a:a):
    asz = arglen(a)
    m = mean(a:asz)
    return(m)
