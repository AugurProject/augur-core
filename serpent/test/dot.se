# Dot (inner) product of vectors.
macro dot($a, $b, $len):
    with i = 0:
        with prod = 0:
            while i < $len:
                prod += $a[i] * $b[i]
                i += 1
            prod

def test_dot(u:a, v:a):
    usz = arglen(u)
    vsz = arglen(v)
    return(dot(u, v, usz))
